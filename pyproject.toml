[project]
name = "mcp-fetch"
version = "0.1.0"
description = "MCP tool for calling local REST API services"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "mcp>=1.0.0",
    "httpx>=0.27.0",
]

[tool.uv]
dev-dependencies = []